.idea/
.claude/settings.local.json
node_modules/

# Versioned build artifacts — only stable aliases (e.g. make-skills.zip,
# make-scenario-building.skill) are committed for raw.githubusercontent.com
# downloads. Versioned archives belong in GitHub Releases, not repo history.
dist/*-v*.zip
dist/*-v*.skill
